$(cat package.json | python3 -c "
import json, sys
pkg = json.load(sys.stdin)
pkg['dependencies']['react-native-reanimated'] = '3.16.7'
del pkg['dependencies']['react-native-worklets-core'] if 'react-native-worklets-core' in pkg['dependencies'] else None
print(json.dumps(pkg, indent=2))
")
